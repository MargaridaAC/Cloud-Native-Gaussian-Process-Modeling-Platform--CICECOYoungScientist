[tool.ruff]
exclude = [
    "original",
    "scratch",
    "venv",
    ".venv",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["original", "scratch", "venv", ".venv"]
addopts = "-v --ignore=scratch --ignore=original --ignore=venv --ignore=.venv"

