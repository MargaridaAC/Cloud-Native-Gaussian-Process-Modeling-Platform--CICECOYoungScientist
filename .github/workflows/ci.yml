name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  PYTHONPATH: .
  TF_USE_LEGACY_KERAS: "1"
  TF_ENABLE_ONEDNN_OPTS: "0"

jobs:
  lint-and-test:
    name: Lint & Python Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip setuptools packaging
          pip install ruff
          pip install -r requirements.txt

      - name: Lint and Syntax Check with Ruff
        run: |
          ruff check . --select E,F,W --ignore E501

      - name: Verify Python Server Import
        run: |
          python -c "import server; print('>>> SERVER IMPORT SUCCESSFUL <<<')"

      - name: Run Pytest Unit Tests
        run: |
          python -m pytest tests/ -v --tb=long -s


  docker-build:
    name: Docker Build Test
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: gp-training-app:latest
